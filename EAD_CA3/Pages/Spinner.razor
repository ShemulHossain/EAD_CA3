<div class="spinner-container">
    <div class="spinner"></div>
    <p>Loading, please wait...</p>
</div>

<style>
    /* Spinner Container */
    .spinner-container {
        display: flex;
        flex-direction: column; /* Stack spinner and text vertically */
        justify-content: center;
        align-items: center;
        height: 100%;
        text-align: center;
    }

    .spinner-container p {
        margin-top: 10px;
        font-size: 1rem;
        color: #555;
    }

    /* Spinner Animation */
    .spinner {
        width: 50px;
        height: 50px;
        border: 6px solid rgba(0, 0, 0, 0.1); /* Lighter border */
        border-top-color: #007bff; /* Bright blue for visual appeal */
        border-radius: 50%;
        animation: spin 0.8s linear infinite; /* Faster spin for modern effect */
    }

    /* Responsive Adjustments */
    media (max-width: 768px) {
        .spinner {
            width: 40px;
            height: 40px;
            border-width: 5px;
        }
    }

    /* Smooth Spin Animation */
    keyframes spin {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }
</style>
